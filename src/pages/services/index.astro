---
import BaseLayout from "../../layouts/BaseLayout.astro";
import Breadcrumbs from "../../components/ui/Breadcrumbs.astro";
import ServicesHubHero from "../../components/sections/ServicesHubHero.astro";
import ServicesScenarios from "../../components/sections/ServicesScenarios.astro";
import { breadcrumbSchema } from "../../utils/schema";
import { composeTitle } from "../../utils/seo";

const title = composeTitle("Vehicle Recovery Services in London + M25");
const description = "Vehicle recovery services across London + M25 — car, van, motorcycle, accident, police pound release, specialist transport, repossession, prestige transport and secure storage. PAS43 certified, 24/7. Call now.";
---

<BaseLayout
  title={title}
  description={description}
  canonicalPath="/services/"
  schema={breadcrumbSchema([
    { label: "Home", path: "/" },
    { label: "Services", path: "/services/" },
  ])}
>
  <div class="container-page breadcrumb-band">
    <Breadcrumbs items={[{ label: "Home", href: "/" }, { label: "Services" }]} />
  </div>

  <ServicesHubHero />

  <ServicesScenarios />
</BaseLayout>
